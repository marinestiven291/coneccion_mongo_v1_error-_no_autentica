/*const {Schema,model}= require('mogoose');

const noteschema=new Schema({
    title : {
        type : 'String',
        required: true
    },
    description :{
        type:'String',
        required:true
    }
}, {timestamps: true
})


module.exports=model('note',noteschema);
*/
